# Bloqueia qualquer acesso HTTP direto a esta pasta.
# Sem isso, um servidor Apache mal configurado poderia servir config.php
# como texto puro, expondo credenciais do banco de dados.
Require all denied
